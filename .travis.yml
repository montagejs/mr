language: node_js
cache:
  directories:
    - node_modules
node_js:
  - "10"
  - "8"
  - "6"
  - "4"
before_script:
  - npm run lint
script: npm run $COMMAND
env:
  - COMMAND=lint
  - COMMAND=test
  - COMMAND=test:mr
  - COMMAND=test:karma
  - COMMAND=integration MOP_VERSION=latest
  - COMMAND=integration MOP_VERSION="#master"
notifications:
  irc:
    channels:
      - "chat.freenode.net#montage"
    on_success: false
    template:
      - "%{author} broke the %{repository} tests on %{branch}: %{build_url}"
